environment:
  matrix:
    - nodejs_version: "10.12"
platform: Any CPU
matrix:
  fast_finish: true
install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm install
test_script:
  - npm run test
build: off
